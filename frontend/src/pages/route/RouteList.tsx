import { RouteCallbacks, RouteStates } from "./types"

type PropsType = {
    callbacks : RouteCallbacks,
    states: RouteStates
}

const RouteList = ({callbacks, states}: PropsType) => {

}

export default RouteList